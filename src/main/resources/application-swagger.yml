springdoc:
  # Включение автоматической генерации OpenAPI документации (/v3/api-docs)
  api-docs.enabled: true
  # Включение веб-интерфейса Swagger
  swagger-ui:
    enabled: true
  path: /swagger-ui/index.html
